{
  "name": "html-bundler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Production HTML5 bundler: fold index.html + styles.css + script.js into one static page. Cloudflare Worker + Durable Objects + R2. Studio export target for html-studio.registermysite.com.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types",
    "test": "node --experimental-strip-types --test test/bundler.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250808.0",
    "wrangler": "^4.78.0"
  }
}
